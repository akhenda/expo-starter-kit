import {{camelCase name}}, { {{pascalCase name}}Service } from './{{pascalCase name}}';

describe('{{pascalCase name}} service', () => {
  it('should say hello world', () => {
    expect({{camelCase name}}.greet()).toBe('Hello, World!');
  });

  it('allows a different greeting', () => {
    const howdy = new {{pascalCase name}}Service('Howdy', 'Folks');

    expect(howdy.greet()).toBe('Howdy, Folks!');
  });
});
