import Button from 'react-native-ui-lib/button';
import Text from 'react-native-ui-lib/text';
import View from 'react-native-ui-lib/view';
import { createDrawerNavigator } from '@react-navigation/drawer';

import type { {{pascalCase name}}DrawerParamList, {{pascalCase name}}DrawerScreenProps } from './{{pascalCase name}}Navigator.types';

const Drawer = createDrawerNavigator<{{pascalCase name}}DrawerParamList>();

const Screen1 = ({ navigation }: {{pascalCase name}}DrawerScreenProps<'Screen One'>) => (
  <View>
    <Text>Screen 1</Text>
    <Button label="Next" onPress={() => navigation.navigate('Screen Two', { parent: 'Screen Ya Kwanza' })} />
  </View>
);

const Screen2 = ({ navigation, route }: {{pascalCase name}}DrawerScreenProps<'Screen Two'>) => (
  <View>
    <Text>Screen 2</Text>
    <Text>{route.params.parent}</Text>
    <Button label="Back" onPress={() => navigation.goBack()} />
  </View>
);

const {{pascalCase name}}Navigator = () => {
  return (
    <Drawer.Navigator>
      <Drawer.Screen name="Screen One" component={Screen1} />
      <Drawer.Screen name="Screen Two" component={Screen2} />
    </Drawer.Navigator>
  );
}

export default {{pascalCase name}}Navigator;
