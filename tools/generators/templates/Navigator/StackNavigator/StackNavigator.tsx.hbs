import Button from 'react-native-ui-lib/button';
import Text from 'react-native-ui-lib/text';
import View from 'react-native-ui-lib/view';
import { createStackNavigator } from '@react-navigation/stack';

import type { {{pascalCase name}}StackParamList, {{pascalCase name}}StackScreenProps } from './{{pascalCase name}}Navigator.types';

const Stack = createStackNavigator<{{pascalCase name}}StackParamList>();

const Screen1 = ({ navigation }: {{pascalCase name}}StackScreenProps<'Screen One'>) => (
  <View>
    <Text>Screen 1</Text>
    <Button label="Next" onPress={() => navigation.navigate('Screen Two', { parent: 'Screen Ya Kwanza' })} />
  </View>
);

const Screen2 = ({ navigation, route }: {{pascalCase name}}StackScreenProps<'Screen Two'>) => (
  <View>
    <Text>Screen 2</Text>
    <Text>{route.params.parent}</Text>
    <Button label="Back" onPress={() => navigation.goBack()} />
  </View>
);

const {{pascalCase name}}Navigator = () => {
  return (
    <Stack.Navigator>
      <Stack.Screen name="Screen One" component={Screen1} />
      <Stack.Screen name="Screen Two" component={Screen2} />
    </Stack.Navigator>
  );
}

export default {{pascalCase name}}Navigator;
