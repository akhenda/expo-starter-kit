import React from 'react';
import Text from 'react-native-ui-lib/Text';
import View from 'react-native-ui-lib/View';

import { $container } from './{{pascalCase name}}.styles';
import type { {{pascalCase name}}Props } from './{{pascalCase name}}.types';

const {{pascalCase name}} = ({ style: $style }: {{pascalCase name}}Props): React.ReactElement => {
  return (
    <View style={[$container, $style]}>
      <Text>{{pascalCase name}}</Text>
    </View>
  );
};

export default {{pascalCase name}};