import type { ImageStyle } from 'react-native';

const $headerImage: ImageStyle = {
  aspectRatio: 1,
  height: 'auto',
  marginRight: 15,
  resizeMode: 'contain',
  width: 30,
};

export default {
  $headerImage,
};
