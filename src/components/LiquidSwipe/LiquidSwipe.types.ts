import type { StyleProp, ViewProps, ViewStyle } from 'react-native';

export interface LiquidSwipeProps extends ViewProps {
  /**
   * Extra styles for the container
   */
  style?: StyleProp<ViewStyle>;
}
